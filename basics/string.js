/*
    Never concatenate a string using "+" sign insted use the
    format below
*/

const name = "Hitesh"
// const repocount = 50
// console.log(`Hello my name is ${name} and my repo count is ${repocount}`);

// // we may declare a string using the method below:

// const new_name = new String("Hitesh");
// console.log(`Hello my name is ${new_name} and my repo count is ${repocount}`)

// console.log(new_name[1]);
// console.log(new_name.toLocaleUpperCase());
// console.log(new_name.toUpperCase());
// console.log(new_name);

// const variable = new_name.substring(0,4)
// console.log(variable);

// const another_string = new_name.slice(-8,2)
// console.log(another_string);

// ".trim()" is used to remove starting and ending spaces of the string
// ".replace(a,b)" is used to remove "a" and replace it to "b"
// ".includes(a)" is used to ask if "a" is in the string
// ".split("/")" it will split the stiring on every "/"
